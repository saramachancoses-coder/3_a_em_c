let somaNotas = 0;

for (let i = 1; i <= 5; i++) {
    let nota = 8; // Simula o usuário digitando uma nota a cada rodada
    somaNotas += nota;
}

let media = somaNotas / 5;
console.log("A média aritmética da turma é: " + media);
